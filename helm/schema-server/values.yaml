name: schema-server

image:
  registry: quay.io
  name: giantswarm/schema-server
  tag: v0.0.1
  pullPolicy: IfNotPresent

hostnames:
  - schema.giantswarm.io

podSecurityStandards:
  enforced: false
